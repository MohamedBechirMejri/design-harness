# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Design Harness (for example via API key or ChatGPT auth supported by Codex).
- Design Harness starts the server via `codex app-server` per session.
